# Corporate Knowledge Base Automation PoC

This PoC has moved out of the shared `pocs` workspace and should live as its own repository.

- Local working copy: `C:\Users\<your-user>\KB`
- Future repo link: replace this with `[corporate-knowledge-base-poc](https://github.com/<your-org>/corporate-knowledge-base-poc)` after you create it.
- Original purpose: enterprise knowledge-base automation with lightweight S3 + Bedrock, medium vector DB and heavier fine-tuning/private-runtime tiers.

Use the standalone repo for code, tests, CI/CD examples, VPC diagrams and implementation docs. This directory is intentionally only a pointer.
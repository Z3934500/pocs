# /etc/knowledge-cockpit.env
# Keep this file on the server only. Do not commit real secrets.
OPENAI_API_KEY=sk-replace-me
OPENAI_MODEL=gpt-4.1-mini
OPENAI_RESPONSES_URL=https://api.openai.com/v1/responses
# Optional but recommended for public deployments. Required for private phone notes and AI KB.
PRESENTER_PIN=change-me-before-demo
